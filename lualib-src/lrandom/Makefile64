CC = gcc
CFLAGS = -g -O2 -Wall
SHARED := -fPIC --shared
LUA_CLIB_PATH ?= luaclib
TARGET = lrandom64.so

.PHONY : all clean

all : $(TARGET)

$(TARGET) : lrandom64.c
	$(CC) $(CFLAGS) $(SHARED) $^ -o $@

clean :
	rm -f $(TARGET)