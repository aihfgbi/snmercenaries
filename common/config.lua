DEBUG = true 		--debug模式，将关闭登录验证

-- USE_QQ_LOGIN = true --使用页面登录
LOGIN_FLAG = "FH" --飞火登录:FH；QQ登录:QQ；不填写默认表示QQ

CAN_RECONNECT_TO_GAME = true --是否允许断线重连

-- USE_FOR_NEW_CLIENT = true --是否是为新客户端

-- PREVENT_ADDICITED = require("dbs_config").PREVENT_ADDICITED

TEST_MODEL = true   --jisong要求添加额外测试标记

ENCRYPT_FALG = false

CONSOLE_LOG = true 	--控制台是否输出日志
LOG_LEVEL = 0 		--文件日志的登记 0 debug; 1 info; 2 warning; 3 error
LOG_PATH = "log"